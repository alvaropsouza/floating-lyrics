# Floating Lyrics — Python dependencies
#
# Install with:  pnpm dlx -- pipx run pip install -r requirements.txt
# or simply:     pip install -r requirements.txt
#
# Requires Python 3.10+

# UI framework
PyQt6>=6.4.0

# WASAPI loopback audio capture via FFmpeg (Windows)
# Instalar: winget install ffmpeg  (ou baixar em https://ffmpeg.org/download.html)
# ffmpeg deve estar no PATH — sem dependência Python

# HTTP client for AudD, lrclib.net, Musixmatch
requests>=2.31.0

# AcoustID: Audio fingerprinting via Chromaprint + MusicBrainz  
# Free, unlimited API (requires free API key from https://acoustid.org/)
pyacoustid>=1.3.0

# WebSocket server para comunicação com Flutter frontend
aiohttp>=3.9.0

# Fast numeric audio processing (RMS, normalization — replaces pure-Python struct loops)
numpy>=1.26.0

# Resample de alta qualidade para STT (44100→16000 Hz sem aliasing)
scipy>=1.11.0

# Logs coloridos no terminal
rich>=13.0.0

# Auto-reload ao salvar arquivos (python main_server_headless.py --reload)
watchfiles>=0.21.0
